= simple_form_for(@user) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, autofocus: true, input_html: { size: 30 }
    = f.input :email

  %br
  .form-actions
    = f.button :submit
