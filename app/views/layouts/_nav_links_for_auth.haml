- if user_signed_in?
  %li= link_to "Sign out #{current_user.github_nickname}", signout_path
- else
  %li= link_to 'Sign in', signin_path
- if user_signed_in?
  - if current_user.try(:admin?)
    %li.admin-only= link_to 'Users', users_path
